# Recorded build 42926 and shared propeller verification

Date: 13 September 2026.

This receipt records the initial API and propeller package checks below.
The later [documentation audit](API_DOC_AUDIT_42926.md) covers the current 31-method reference and all bundled skills.
Its offline checks do not replace or relabel the native evidence recorded here.

- All ten demo smoke commands passed in the fresh staged copy.
- The complete test suite passed: 102 tests.
- A fresh export of the staged tree in a path containing spaces passed all ten smoke commands and all 102 tests.
- The public-payload audit found zero findings across 957 files and 972 local links.
- The audit round-tripped 19 native notebooks and scanned image metadata and embedded/compressed payloads.
- Twenty-three propeller graphs passed hash, connection, asset, and local-output-path checks.
- The two included native assembly notebooks reproduce their extracted public graphs exactly.
- API value/unit behavior is recorded source evidence. The optional probe and TCP transport have offline tests; they were not newly executed against nTop from this public checkout.
- Propeller screenshots and measurements identify their recorded native source revisions. Case 10 now carries the half-turn revision and explicitly notes its visible edge ripples.

Browser policy blocked opening the shared report's local file URL. Browser visual verification of this new gallery is incomplete. Static resource and image checks passed.

No remote publication is part of this verification record. Cloud credentials, licensed solver binaries, source photographs, and generated CFD output are excluded.

The marine case records 147 successful native variables and a 504,484-triangle watertight screw. Its 38 filtered fragments have a 1.1434 mm maximum distance bound, and retained vertices are exactly unchanged. Four native views were visually inspected. The separate cap has eight M24 screws; the shaft-specific spline and axial propeller retainer remain pending.

The fresh-copy test run passed all 102 tests after the smoke builds generated their required recipe outputs. The first pre-build run skipped ten such checks. The source marine HTML report was visually inspected in the browser; the public gallery preview remains subject to the browser restriction above.
