# Build 42926 and shared propeller verification

Date: 13 September 2026.

- All ten demo smoke commands passed in the main checkout.
- The complete test suite passed: 100 tests.
- A fresh export of the staged tree in a path containing spaces passed all ten smoke commands and all 100 tests.
- The public-payload audit found zero findings across 943 files and 964 local links.
- The audit round-tripped 19 native notebooks and scanned image metadata and embedded/compressed payloads.
- Twenty-two propeller graphs passed hash, connection, asset, and local-output-path checks.
- The two included native assembly notebooks reproduce their extracted public graphs exactly.
- API value/unit behavior is recorded source evidence. The optional probe and TCP transport have offline tests; they were not newly executed against nTop from this public checkout.
- Propeller screenshots and measurements identify their recorded native source revisions. Case 10 now carries the half-turn revision and explicitly notes its visible edge ripples.

Browser policy blocked opening the shared report's local file URL. Browser visual verification of this new gallery is incomplete. Static resource and image checks passed.

No remote publication is part of this verification record. Cloud credentials, licensed solver binaries, source photographs, and generated CFD output are excluded.
